# .env.example
# Copy this file to ".env" and point it at your deployed API's URL.
# Vite only exposes variables prefixed with VITE_ to the browser.

VITE_API_BASE_URL="http://localhost:4000/api/tasks"
